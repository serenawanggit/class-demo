<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yanyi.dao.CourselistDao">
	<resultMap id="BaseResultMap" type="com.yanyi.entity.CourselistDto">
		<!-- WARNING - @mbggenerated This element is automatically generated by 
			MyBatis Generator, do not modify. This element was generated on Fri Oct 18 
			14:33:48 CST 2019. -->
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="name" property="name" jdbcType="VARCHAR" />
		<result column="course_id" property="course" jdbcType="INTEGER" />
		<result column="datail" property="datail" jdbcType="VARCHAR" />
	</resultMap>
	<!-- 查询所有课程信息 -->
	<select id="queryAll" resultType="com.yanyi.entity.CourselistDto">
		SELECT
		id,
		name,
		course_id courseId,
		(
		SELECT
		NAME
		FROM
		study_course
		WHERE
		id = t.course_id
		) course
		FROM
		study_course_list t
		WHERE
		1 = 1
		<if test="id!=null and id!=''">
			AND id=#{id}
		</if>
	</select>
	<delete id="delCourseById" parameterType="string">
		DELETE
		FROM
		study_course_list
		WHERE
		id IN(${value})
	</delete>
	
	<select id="queryCourse" resultType="com.yanyi.entity.CourselistDto" 
	parameterType="com.yanyi.entity.CourselistDto">
		select id,name,video,image,price,course_type_id courseTypeId,is_flag
		isFlag from study_course WHERE 1=1
		<if test="id!=null and id!=''">
			AND id=#{id}
		</if>
		<if test="name!=null and name!=''">
			AND name like '%${name}%'
		</if>
	</select>
	<!-- 更新 -->
	<update id="updateCourseById" >
		UPDATE
		study_course_list
		<set>
			<if test="name!=null">
				name=#{name},
			</if>
			<if test="courseId!=null">
				course_id=#{courseId}
			</if>
		</set>
		where
		id=#{id}
	</update>
	<insert id="addCourse" parameterType="com.yanyi.entity.CourselistDto">
			INSERT INTO study_course_list (name,course_id)
			VALUES (#{name},#{courseId})
	</insert>
	
	<select id="queryCourseListByParam" resultType="com.yanyi.entity.CourselistDto" parameterType="com.yanyi.entity.CourselistDto">
		SELECT id,name,course_id courseId, video FROM study_course_list t
		<where>
			<if test="id!=null">
				And id = #{id}
			</if>
			<if test="name!=null">
				And id like '%${id}%'
			</if>
			<if test="courseId!=null">
				And course_id = #{courseId}
			</if>
		</where>
	</select>
</mapper>